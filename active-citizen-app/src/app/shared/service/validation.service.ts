export class ValidationService {
  //  CLASS / SERVICE TO BE DELETED
  // public static getValidationErrorMessage(validatorName: string, labelName?: string, validatorValue?: any, ): any {
  //   const config = {
  //     required: `${labelName || 'Field'} is required.`,
  //     invalidPassword: 'Invalid password. Password must be at least 6 characters long, and contain a number.',
  //     maxlength: `${labelName || 'The field'} can't contain more than ${validatorValue.requiredLength} characters.`,
  //     minlength: `${labelName || 'The field'} must contain atleast ${validatorValue.requiredLength} characters.`
  //   };

  //   return config[validatorName];
  // }
}
